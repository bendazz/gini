Static web app for visualizing the decision tree and Gini impurity info.

- Open index.html in a browser (or use a simple HTTP server).
- Expects ../tree_data.json to be present (generated by export_tree_gini.py).
- Click nodes to see Gini calculation and sample grid in the sidebar.
